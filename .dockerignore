# Секреты и локальное окружение
.env
*.env.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# Git / IDE / OS
.git/
.gitignore
.idea/
.vscode/
.DS_Store

# Артефакты, не нужные в образе
demo/DB_description.xlsx
benchmark/results/

# chroma_db/ намеренно НЕ игнорируем: 
# если локально собранный RAG-индекс есть, то он попадёт в образ и переиспользуется. 
# иначе индекс соберётся при первом старте приложения (warmup -> build_index_if_empty).