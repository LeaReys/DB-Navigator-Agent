"""
web = тонкий HTTP-слой над агентом DB Navigator.

Назначение пакета — отдать готовый граф LangGraph наружу через HTTP,
не меняя саму логику агента:

  - server.py  — FastAPI-приложение (SSE-чат, health-check, статика);
  - events.py  — перевод обновлений узлов графа в события для UI.

CLI-режимы (app.py) и веб-режим используют один и тот же build_graph(),
поэтому поведение агента в консоли и в браузере идентично.
"""
